spring:
  application:
    name: ai-code
server:
  port: 8088
  servlet:
    context-path: /api

# springdoc-openapi????
springdoc:
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.xc.java.aicode.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn